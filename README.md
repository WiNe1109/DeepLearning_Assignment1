# DeepLearning_Assignment1
# Single Layer Perceptron - Iris Dataset
Python implementation of a Single Layer Perceptron for binary classification (Setosa = 0, Versicolor = 1).

## How to Run
1. Install dependencies:
   pip install pandas numpy matplotlib scikit-learn openpyxl
2. Run the script:
   python slp_excel.py
